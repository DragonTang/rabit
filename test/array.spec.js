const { expect } = require('chai')
const { unique } = require('../src/array')

describe('unique', () => {
  it('should return [1,2,3,4,5,6] when array is [1,1,2,3,4,5,6,3,5]', () => {
    // expect(sum([1,1,2,3,4,5,6,3,5])).to.equal([])
    // console.log(unique([1,1,2,3,4,5,6,3,5]))
  })
})
